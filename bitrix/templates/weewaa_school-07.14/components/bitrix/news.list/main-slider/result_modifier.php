<?/*
foreach($arResult["ITEMS"] as &$arItem):
	if (count($arItem["DISPLAY_PROPERTIES"]["MORE_PHOTO"]["VALUE"]) > 0) {
		foreach ($arItem["DISPLAY_PROPERTIES"]["MORE_PHOTO"]["VALUE"] as $cell=>$PHOTO) {
			$arItem['SLIDER'][$cell] = array_change_key_case(CFile::ResizeImageGet($PHOTO["ID"], array("width"=>390, "height"=>500), BX_RESIZE_IMAGE_EXACT, true), CASE_UPPER);
		//echo'<pre>'; print_r($PHOTO); echo '</pre>';
		}
	}
	 //echo'<pre>'; print_r($arItem['SLIDER']); echo '</pre>';
endforeach;
unset($arItem);*/
?> 

<?
/*if(CModule::IncludeModule("iblock"))
   {


   }
	if ($USER->IsAdmin()) { 
	 echo'<pre>'; print_r($arResult); echo '</pre>';
	}*/
?> 